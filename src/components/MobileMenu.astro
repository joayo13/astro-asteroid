---
import { LINKS } from '@consts';
import slugify from '@utils/slugify';
---

<ul
    tabindex="0"
    class="menu menu-lg dropdown-content bg-base-100 rounded-box w-52 mt-3 z-10 p-2 shadow"
>
    {
        LINKS.map((link) =>
            link.SUBLINKS ? (
                <li>
                    <details>
                        <summary>{link.TITLE}</summary>
                        <ul class="p-2">
                            {link.SUBLINKS.map((sublink) => (
                                <li>
                                    <a href={slugify(sublink.TITLE)}>
                                        {sublink.TITLE}
                                    </a>
                                </li>
                            ))}
                        </ul>
                    </details>
                </li>
            ) : (
                <li>
                    <a href={slugify(link.TITLE)}>{link.TITLE}</a>
                </li>
            )
        )
    }
</ul>
